start
declare X = 10;
declare Y = -25;
declare Z = +100;
declare F = 3.14;
declare G = -2.5e10;
declare H = +6.022e23;

declare FLAG = true;
declare DONE = false;

declare STR = "Hello World";
declare ESC = "Line\nTab\tQuote\"Backslash\\";
declare CH1 = 'a';
declare CH2 = '\n';
declare CH3 = '\\';

X++;
Y--;
X += 5;
Y -= 3;
X *= 2;
Y /= 4;

IF(X >= Y && FLAG || !DONE) {
    output(X);
}
else {
    input(Y);
}

loop (X < 100) {
    X = X + 1;
    continue;
    break;
}

function MyFunc() {
    return X ** 2;
}

finish
