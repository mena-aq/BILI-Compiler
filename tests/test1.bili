#* BLOCK COMMENT TEST: 
   This should be recognized as a single COMMENT token.
*#z

start {
    /* Valid Declarations */
    declare INT Alpha = 100;
    declare FLOAT Beta = 3.1415;
    declare STRING Greeting = "Hello, Lexer!";
    declare BOOLEAN IsActive = true;

    ## Error 1: Invalid Identifier (Starts with lowercase)
    declare INT count = 5; 

    ## Error 2: Malformed Float (Multiple decimals)
    declare FLOAT BadNum = 10.5.2;

    ## Error 3: Identifier Too Long (Exceeds 31 chars)
    declare INT ThisIsAVeryLongIdentifierThatShouldExceedTheLimit = 99;

    ## Error 4: Unterminated String
    declare STRING Broken = "Forgot to close this;

    /* Testing Multi-character Operators */
    Alpha = Alpha ** 2;    ## Power
    Alpha += 10;           ## Add-assign
    
    condition (Alpha >= 100 && IsActive == true) {
        output("Success");
    } else {
        loop (Alpha > 0) {
            Alpha--;
            break;
        }
    }

    ## Error 5: Unclosed Multi-line Comment
    #* This comment never ends...
    
    return 0;
}
finish